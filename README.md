# 👓 Adaptive Vision Reader

An adaptive, glasses-free reading application utilizing real-time pupillary tracking, dynamic typography scaling, OpenCV spatial edge sharpening, and native browser Text-to-Speech (TTS).

---

## 🚀 Local Setup

1. **Clone the repository:**
   ```bash
   git clone [https://github.com/your-username/adaptive-reader.git](https://github.com/your-username/adaptive-reader.git)
   cd adaptive-reader
